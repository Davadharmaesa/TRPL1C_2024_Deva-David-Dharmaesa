import 'dart:io';

int faktorialrekursif (int x){
  if (x == 1){
    return x;
  }else{
    return x * faktorialrekursif(x - 1);
  }
}

void main(){
  stdout.write("N = ");
  int n = int.parse(stdin.readLineSync()!);
  print("Hasil = ${faktorialrekursif(n)}");
}
